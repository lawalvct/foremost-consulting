# Codex / AI Development Instructions

Before changing this repository, read `docs/FOREMOST_WEBSITE_CODEX_INSTRUCTIONS.md` completely.

That document is the authoritative product and engineering brief. Work in small, testable phases; preserve existing work; do not invent company facts; use `[TO CONFIRM]` where management confirmation is required; and never add Docker, Laravel Sail, Docker Compose, Next.js, Nuxt, or a separate API application.

For the first implementation session:

1. Inspect the fresh Laravel Vue starter repository.
2. Confirm versions and current automated checks.
3. Configure the Foremost brand tokens and assets.
4. Build only the shared public layout, responsive header, footer and initial homepage shell.
5. Run formatting, type checks, PHP tests and the production SSR build.
6. Report changed files, test results and remaining Phase 1 work.
